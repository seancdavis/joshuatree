jQuery(document).ready(function($){	
	// Thumbnail Tiles
	/*if( $('.rt-tile-thumb').length > 0 ) {
		$('.rt-tile-thumb p img').each(function(){
			var height = parseFloat( $(this).css('height').substr(0,$(this).css('height').length - 2) );
			$(this).parent().append('<div class="thumb-cover" style="height:' + height + 'px;"></div>');
		});
		
		$('.rt-tile-thumb p img').hover(function(){			
			$(this).parent().children('.thumb-cover').show();
		}, function(){
			//$(this).parent().children('.thumb-cover').hide();			
		});
	}*/	
});
